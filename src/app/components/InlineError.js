import React from 'react';

const InlineError = (props) => {
    return(
        <div>
            <small id="emailHelp" class="form-text text-muted">{this.props.errors}</small>
        </div>
    )
}

export default InlineError;